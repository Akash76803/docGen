import {createBlankArtboard,createShapeElement,addDesignElement,groupElements,expandElementIdsToGroups,moveLayer,orderedLayers,scaleElements,rotateElementsAsGroup} from '../packages/design-engine/dist/index.js';
let t={kind:'CARD_DESIGN',schemaVersion:1,id:'smoke',name:'Phase 6.0.5',version:1,status:'DRAFT',sharedAssets:[],artboards:[createBlankArtboard({id:'a',name:'Front',order:0,widthMm:90,heightMm:50})]};
t=addDesignElement(t,'a',createShapeElement('RECTANGLE',{id:'one',zIndex:0,xMm:10,yMm:10}));t=addDesignElement(t,'a',createShapeElement('RECTANGLE',{id:'two',zIndex:1,xMm:40,yMm:10}));t=groupElements(t,'a',['one','two'],'g1','Group 1');const expanded=expandElementIdsToGroups(t.artboards[0],['one']);t=moveLayer(t,'a','one','FRONT');t=scaleElements(t,'a',expanded,1.1);t=rotateElementsAsGroup(t,'a',expanded,15);console.log(JSON.stringify({status:'PASS',phase:'6.0.5',groupMembers:expanded,layers:orderedLayers(t.artboards[0]).map(e=>e.id),groupCount:t.artboards[0].groups.length},null,2));
